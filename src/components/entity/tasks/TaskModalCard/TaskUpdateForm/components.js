import makeStyles from '@material-ui/styles/makeStyles'

export const useStyle = makeStyles(() => ({
  root: {},

  radioGroup: {
    marginBottom: '10px',
    marginLeft: '5px'
  },

  radioLabel: {
    color: 'white',
    fontWeight: 'bold'
  },

  radioButton: {
    color: 'white'
  }
}))
